<div id="#loadResultDisplay">
    <div class="wrapper">

        {{>nav}}

     {{>header}}

        <div>
            <input placeholder="Search...." id="loadNum" type="text" />
            <button id="loadSrch" type="search" class="btn btn-success">Create Author</button>
        </div>



        <div id="results-list">
            {{!-- <div class="p-2 m-2 border rounded" data-id="{{dataValues}}">
		
		<p>Broker: {{dataValues.broker}} Load Number: {{dataValues.loadNum}}</p>
        <p>Pick Up Address: {{dataValues.puAddress}} Drop Off Address: {{dataValues.doAddress}}</p>
        <p>Pick Up Date: {{dataValues.puDate}}</p>
        <p>Drop Off Date: {{dataValues.dueDate}}</p>
        <p>Trailer#:{{dataValues.trailer}}</p>
        <p>Driver: {{Driver.dataValues.driverName}}</p>
        <input class="future" type="radio" id="status" name="{{dataValues.loadNum}}" value="future">
        <label for="future">Future</label>
        <input class="enRoute" type="radio" id="status" name="{{dataValues.loadNum}}" value="enRoute">
        <label for="enRoute">En Route</label>
        <input class="delivered" type="radio" id="status" name="{{dataValues.loadNum}}" value="delivered">
        <label for="delivered">Delivered</label>
	</div>--}}
</div> 


        </div>
    </div>

</div>

{{>footer}}

<script src="https://cdnjs.cloudflare.com/ajax/libs/handlebars.js/4.7.6/handlebars.min.js"></script>
<script src='/assets/js/search.js'></script>